keys/ is excluded from the image intentionally — mount those at runtime via a volume or secrets manager (e.g. AWS Secrets Manager / ECS secrets)

PGPASSWORD='yourpassword' pg_dump -U ruralpayuser -h localhost -d ruralpay > backup.sql